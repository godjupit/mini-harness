    # Mini OpenHarness 小白源码教材：总目录与学习方法

    > 适用对象：刚开始学习 Python、第一次接触 Agent（智能体）运行框架的读者。  
    > 对应项目：`mini-openharness` 0.6.0。  
    > 建议做法：一边打开源码，一边按本章给出的行号和问题阅读。

    ## 1. 这套教材要解决什么问题

很多初学者读源码时会陷入两个极端：

- 从第一个文件第一行开始，逐字逐句读，读了很久仍不知道程序整体在做什么；
- 只看架构图和名词，觉得自己“好像懂了”，但真正打开函数后又无法解释代码为什么这样写。

这套教材采用“先地图、后主路、再支路”的方式。你会先知道程序从哪里启动、数据怎样流动，再逐步学习工具、权限、Hook、Provider、上下文压缩等模块。

本项目可以先记成一句话：

```text
用户给任务 → 模型决定是否调用工具 → 程序安全执行工具 → 结果交回模型 → 直到模型完成回答
```

这里的 Agent 不是一个神秘的新物种。它本质上是：

```text
大语言模型 + 消息历史 + 可调用工具 + 一个反复执行的控制循环
```

## 2. 建议的学习节奏

### 第一轮：只建立主干

先读第 01～06 章。不要追求记住所有语法，只要能回答：

1. 命令从哪里进入程序？
2. `Message` 和 `ToolCall` 保存什么？
3. `AgentLoop.run()` 为什么会反复调用模型？
4. 工具结果如何重新进入消息历史？

### 第二轮：理解安全和并发

读第 07～10 章。重点回答：

1. 模型为什么不能直接执行任意 Python 函数？
2. 参数校验、权限和 Hook 分别解决什么问题？
3. 多个工具为什么有时并发、有时必须等待？

### 第三轮：理解外部连接和长任务

读第 11～16 章。重点理解 Provider、上下文压缩、Trace、Skill、MCP 和 Docker Sandbox。

### 第四轮：用测试反向验证

读第 17 章，并实际运行测试。源码不是靠“相信作者”理解的，而是靠输入、输出和断言验证的。

## 3. 章节目录

1. `01-先读README建立项目地图.md`
2. `02-从pyproject理解安装与程序入口.md`
3. `03-精读CLI依赖装配与生命周期.md`
4. `04-理解模型Message和ToolCall数据结构.md`
5. `05-用最小测试看懂Agent闭环.md`
6. `06-精读AgentLoop主状态机.md`
7. `07-精读ToolRegistry工具执行边界.md`
8. `08-理解并发执行资源锁与取消.md`
9. `09-理解权限策略与人工审批.md`
10. `10-理解Hooks生命周期扩展.md`
11. `11-理解Provider流式输出重试与错误.md`
12. `12-理解上下文压缩与Artifact.md`
13. `13-理解Trace可观测性与安全回放.md`
14. `14-理解Skill渐进式加载.md`
15. `15-理解MCP工具桥接与OAuth.md`
16. `16-理解DockerSandbox隔离执行.md`
17. `17-用其余测试建立完整心智模型.md`

## 4. 阅读代码时固定使用的五个问题

每打开一个函数，都写下以下答案：

1. **输入是什么？** 参数、对象状态、环境变量还是文件？
2. **输出是什么？** 返回值、事件、文件还是副作用？
3. **状态在哪里改变？** 哪个列表、计数器或文件被修改？
4. **失败如何表示？** 抛异常、返回错误对象，还是写一条消息？
5. **谁调用它？** 上游入口是谁，下游依赖是谁？

例如阅读 `ToolRegistry.execute()`：

```text
输入：工具名、参数、ToolContext
输出：ToolResult
状态：通常由具体 Tool 产生文件副作用
失败：多数失败被转换成 is_error=True 的 ToolResult
调用者：AgentLoop._execute_timed()
```

## 5. 初学者必须先知道的 Python 语法

你不需要先学完整本 Python 教材，但至少要认识：

```python
@dataclass(frozen=True)
class Message:
    role: str
```

- `class`：定义一种对象模板；
- 类型标注 `role: str`：告诉读者和工具“这里应当是字符串”；
- `@dataclass`：自动生成构造函数等样板代码；
- `frozen=True`：对象创建后不允许直接修改字段；
- `async def`：异步函数；
- `await`：等待异步操作完成，同时允许事件循环处理别的任务；
- `async for`：遍历异步数据流；
- `yield`：逐个产生结果，而不是一次性返回全部结果；
- `Protocol`：描述“一个对象只要具备这些属性和方法，就可以被当成这种接口使用”。

## 6. 推荐的实际操作

```bash
unzip mini-openharness-main.zip
cd mini-openharness-main
python -m venv .venv
.venv/bin/pip install -e '.[dev]'
.venv/bin/mini-oh --demo --workspace . "解释这个项目"
.venv/bin/pytest -q
```

第一次运行建议使用 `--demo`，因为它不需要 API Key，而且执行顺序固定，便于观察。

## 7. 学完后的最低目标

学完后，你不需要立刻能从零写出完整框架，但应能：

- 沿着一次运行指出每个关键函数；
- 解释为什么工具错误通常不会让整个 Agent 立即崩溃；
- 解释权限、Hook、Sandbox 的区别；
- 新增一个简单只读工具；
- 通过测试证明修改没有破坏关键不变量。
